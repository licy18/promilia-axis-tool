# 蓝色星原战斗排轴编辑器（Promilia-axis-tool）第三阶段代码层面验收模板
（专供SOLO模式AI执行代码静态与逻辑验收，无需浏览器交互，100%覆盖第三阶段核心功能的代码实现，可追溯、可量化）

---

## 【代码验收核心指令（AI必须严格执行）】
### 你的角色
你是一名专业的前端代码审计工程师，以**文件完整性、逻辑实现度、代码规范性、架构复用性**为原则，对第三阶段开发完成的代码进行全量静态验收。
### 验收规则
1. 必须严格按照本模板顺序，**逐一检查每个文件、每个核心函数、每个关键逻辑点**，不得遗漏
2. 每一项验收仅可填写「✅ 通过」「❌ 不通过」「⚠️ 部分通过」，禁止模糊描述
3. 不通过/部分通过的项，必须在「异常说明」中详细填写：**缺失的文件路径/函数名、未实现的逻辑点、代码中的硬编码/架构问题、与要求的偏差**
4. 验收基于项目根目录的完整代码，通过读取文件内容、分析代码结构、检查依赖配置执行，无需启动服务
5. 禁止为未实现的代码伪造通过结果，所有结论必须有明确的文件/代码片段支撑

---

## 一、前置代码验收（必须全部通过，否则终止后续验收）
| 验收编号 | 验收项 | 验收操作 | 通过标准 | 验收结果 | 异常说明 |
|----------|--------|----------|----------|----------|----------|
| CODE-PRE-001 | 项目依赖完整性 | 1. 读取`package.json`文件<br>2. 检查`dependencies`和`devDependencies`字段 | 1. 包含第三阶段强制依赖：`echarts`、`vue-draggable@next`、`pinia`、`element-plus`、`vue-i18n`等<br>2. 所有依赖版本为稳定兼容版，无`*`通配符<br>3. 无第二阶段依赖缺失 |  |  |
| CODE-PRE-002 | 核心目录结构完整性 | 1. 遍历项目根目录与`src/`目录<br>2. 对比第三阶段强制目录结构 | 1. 所有强制文件夹存在：`src/components/editor/`、`src/components/timeline/`、`src/components/datamanage/`、`src/views/`、`src/utils/`、`public/gamedata/`<br>2. 无空文件夹，所有目录包含基础文件 |  |  |
| CODE-PRE-003 | 第二阶段核心代码复用性 | 1. 读取第二阶段已有的核心文件（`src/store/project.js`、`src/views/Editor.vue`、`src/utils/common.js`等）<br>2. 检查是否有核心架构重构 | 1. 第二阶段核心组件、状态管理、工具函数被完整复用<br>2. 无核心架构推翻/重构，仅做扩展<br>3. 无重复造轮子的代码 |  |  |

---

## 二、分模块代码详细验收
### 模块一：完整伤害计算引擎升级（P0核心）
| 验收编号 | 验收项 | 验收操作 | 通过标准 | 验收结果 | 异常说明 |
|----------|--------|----------|----------|----------|----------|
| DMG-CODE-001 | 核心计算文件存在性 | 检查`src/utils/damageCalc.js`文件是否存在 | 文件存在，无缺失 |  |  |
| DMG-CODE-002 | 全乘区计算函数实现 | 读取`src/utils/damageCalc.js`，检查以下函数是否存在且逻辑完整：<br>1. `calcBaseDamage()`（基础倍率区）<br>2. `calcAttack()`（攻击力区）<br>3. `calcDamageBonus()`（增伤区）<br>4. `calcCritDamage()`（暴击区）<br>5. `calcResistance()`（抗性区）<br>6. `calcLevelPenalty()`（等级压制区）<br>7. `calcToughnessCrit()`（韧性暴击区） | 1. 所有函数存在，函数名与要求一致<br>2. 每个函数有完整的JSDoc注释<br>3. 乘区计算顺序与游戏逻辑一致，无遗漏 |  |  |
| DMG-CODE-003 | 印记体系适配代码 | 检查`damageCalc.js`中是否包含：<br>1. 印记层数增伤/减防计算逻辑<br>2. 印记触发被动联动的判断逻辑<br>3. 印记消耗倍率加成计算 | 1. 印记相关逻辑完整实现<br>2. 逻辑通过`gamedata.json`配置驱动，无硬编码<br>3. 有清晰的注释说明适配的游戏机制 |  |  |
| DMG-CODE-004 | 韧性与DOT机制适配 | 检查`damageCalc.js`中是否包含：<br>1. 技能削韧累加逻辑<br>2. 破韧状态判定与虚弱增伤逻辑<br>3. DOT触发间隔、持续时长、快照机制逻辑 | 1. 所有机制逻辑完整实现<br>2. DOT支持快照模式配置<br>3. 逻辑可扩展，无硬编码数值 |  |  |
| DMG-CODE-005 | 多计算模式支持 | 检查`damageCalc.js`中是否包含：<br>1. `calcExpectedDamage()`（期望模式）<br>2. `calcMaxCritDamage()`（满暴击模式）<br>3. `calcNoCritDamage()`（不暴击模式） | 1. 三种模式函数均存在<br>2. 模式切换逻辑完整<br>3. 函数有入参校验与兜底处理 |  |  |
| DMG-CODE-006 | 队伍联动计算逻辑 | 检查`damageCalc.js`中是否包含：<br>1. 跨角色buff生效判断逻辑<br>2. 元素共鸣效果计算逻辑<br>3. 奇波增益纳入计算逻辑 | 1. 队伍联动逻辑完整<br>2. 支持多角色buff叠加计算<br>3. 逻辑与状态管理中的队伍数据解耦 |  |  |

### 模块二：循环合法性自动校验系统（P0核心）
| 验收编号 | 验收项 | 验收操作 | 通过标准 | 验收结果 | 异常说明 |
|----------|--------|----------|----------|----------|----------|
| VAL-CODE-001 | 校验工具文件存在性 | 检查`src/utils/validate.js`文件是否存在 | 文件存在，无缺失 |  |  |
| VAL-CODE-002 | 核心校验函数实现 | 读取`validate.js`，检查以下函数是否存在：<br>1. `validateCDConflict()`（CD冲突校验）<br>2. `validateResource()`（资源合法性校验）<br>3. `validateDependency()`（技能依赖校验）<br>4. `validateAction()`（动作合法性校验） | 1. 所有校验函数存在<br>2. 每个函数返回统一格式的校验结果（包含问题类型、位置、描述、等级）<br>3. 函数有完整的JSDoc注释 |  |  |
| VAL-CODE-003 | 逐帧资源计算逻辑 | 检查`validateResource()`函数内部：<br>1. 是否有逐帧遍历时间轴的逻辑<br>2. 是否有星决能量、印记、专属资源的变化计算<br>3. 是否有资源缺口判定逻辑 | 1. 逐帧计算逻辑完整<br>2. 资源变化与技能配置联动<br>3. 缺口判定准确，无漏判 |  |  |
| VAL-CODE-004 | 校验状态管理 | 读取`src/store/project.js`，检查是否包含：<br>1. 校验结果的state存储<br>2. 触发校验的action<br>3. 校验结果更新的逻辑 | 1. 校验状态完整集成到Pinia store<br>2. 支持防抖触发校验<br>3. 状态更新响应式，无延迟 |  |  |
| VAL-CODE-005 | 校验面板组件存在性 | 检查`src/components/editor/ValidatePanel.vue`文件是否存在 | 文件存在，无缺失 |  |  |

### 模块三：排轴可视化能力升级（P0核心）
| 验收编号 | 验收项 | 验收操作 | 通过标准 | 验收结果 | 异常说明 |
|----------|--------|----------|----------|----------|----------|
| VIS-CODE-001 | 连携线组件存在性 | 检查`src/components/timeline/LinkLine.vue`文件是否存在 | 文件存在，无缺失 |  |  |
| VIS-CODE-002 | SVG贝塞尔曲线绘制逻辑 | 读取`LinkLine.vue`：<br>1. 检查是否使用SVG绘制曲线<br>2. 检查是否有贝塞尔曲线路径计算逻辑<br>3. 检查是否有「正常/不满足」状态的颜色区分 | 1. SVG绘制逻辑完整<br>2. 曲线连接技能块位置准确<br>3. 状态颜色区分逻辑实现<br>4. 有鼠标悬浮/点击的交互逻辑 |  |  |
| VIS-CODE-003 | 多轨道组件实现 | 读取`src/components/timeline/`目录：<br>1. 检查是否有轨道拆分的子组件<br>2. 检查主轨道是否包含技能、buff、资源3条子轨道 | 1. 轨道组件结构完整<br>2. 子轨道支持折叠/展开、锁定/隐藏<br>3. 轨道样式与主题适配 |  |  |
| VIS-CODE-004 | buff轨道可视化逻辑 | 检查是否有buff轨道相关组件：<br>1. 检查是否有buff色块渲染逻辑<br>2. 检查是否有buff叠加层数显示逻辑<br>3. 检查是否有鼠标悬浮显示详情逻辑 | 1. buff色块渲染完整<br>2. 生效时间与技能配置一致<br>3. 交互逻辑实现 |  |  |
| VIS-CODE-005 | 资源曲线组件存在性 | 检查`src/components/timeline/ResourceCurve.vue`文件是否存在 | 文件存在，无缺失 |  |  |
| VIS-CODE-006 | ECharts曲线绘制逻辑 | 读取`ResourceCurve.vue`：<br>1. 检查是否引入`echarts`<br>2. 检查是否有资源数据处理为图表数据的逻辑<br>3. 检查是否有图表初始化、更新、销毁逻辑 | 1. ECharts集成完整<br>2. 曲线数据与时间轴对齐<br>3. 有窗口resize适配逻辑<br>4. 有鼠标悬浮交互逻辑 |  |  |

### 模块四：数据统计与可视化分析（P0核心）
| 验收编号 | 验收项 | 验收操作 | 通过标准 | 验收结果 | 异常说明 |
|----------|--------|----------|----------|----------|----------|
| STAT-CODE-001 | 统计工具文件存在性 | 检查`src/utils/statCalc.js`文件是否存在 | 文件存在，无缺失 |  |  |
| STAT-CODE-002 | 核心统计指标计算 | 读取`statCalc.js`，检查是否包含：<br>1. 总时长、总伤害、平均DPS计算<br>2. 爆发峰值DPS、总削韧值计算<br>3. buff覆盖率、CD利用率、资源溢出率计算 | 1. 所有统计指标计算逻辑完整<br>2. 计算基于伤害计算引擎的结果<br>3. 函数有完整注释 |  |  |
| STAT-CODE-003 | 统计面板组件存在性 | 检查`src/components/editor/StatPanel.vue`文件是否存在 | 文件存在，无缺失 |  |  |
| STAT-CODE-004 | ECharts图表集成 | 读取`StatPanel.vue`：<br>1. 检查是否有伤害占比饼图逻辑<br>2. 检查是否有DPS时间曲线逻辑<br>3. 检查是否有图表数据更新逻辑 | 1. 饼图、曲线图表完整实现<br>2. 图表数据与统计结果联动<br>3. 支持图表导出高清图片逻辑 |  |  |
| STAT-CODE-005 | 统计报告生成逻辑 | 检查是否有Markdown报告生成函数：<br>1. 检查是否有报告内容拼接逻辑<br>2. 检查是否有格式规范处理 | 1. 报告生成逻辑完整<br>2. 报告包含排轴、伤害、校验、循环质量内容<br>3. 格式符合Markdown规范 |  |  |

### 模块五：内置游戏数据编辑器（P0核心）
| 验收编号 | 验收项 | 验收操作 | 通过标准 | 验收结果 | 异常说明 |
|----------|--------|----------|----------|----------|----------|
| EDIT-CODE-001 | 数据编辑器页面存在性 | 检查`src/views/DataEditor.vue`文件是否存在 | 文件存在，无缺失 |  |  |
| EDIT-CODE-002 | 路由配置完整性 | 读取`src/router/index.js`，检查是否有`/editor`路由配置 | 路由存在，指向`DataEditor.vue`，无权限限制 |  |  |
| EDIT-CODE-003 | 数据管理状态实现 | 读取`src/store/gamedata.js`，检查是否包含：<br>1. 角色、技能、奇波、敌人的state存储<br>2. 新增、编辑、删除数据的action<br>3. 数据合法性校验逻辑 | 1. 数据状态管理完整<br>2. 支持数据的CRUD操作<br>3. 校验逻辑完整，禁止非法数据保存 |  |  |
| EDIT-CODE-004 | gamedata.json生成逻辑 | 检查`DataEditor.vue`或`gamedata.js`中是否包含：<br>1. 数据序列化为JSON的逻辑<br>2. 文件下载触发逻辑<br>3. 数据版本标记逻辑 | 1. JSON生成逻辑完整，格式正确<br>2. 下载功能实现<br>3. 包含版本、游戏版本、更新时间字段 |  |  |
| EDIT-CODE-005 | 外部数据导入逻辑 | 检查是否有文件读取逻辑：<br>1. 检查是否有文件选择触发逻辑<br>2. 检查是否有JSON解析与数据加载逻辑<br>3. 检查是否有非法文件拦截逻辑 | 1. 导入逻辑完整<br>2. 支持JSON文件解析<br>3. 有错误提示与兜底处理 |  |  |

### 模块六：项目导入导出与社区分享体系（P0核心）
| 验收编号 | 验收项 | 验收操作 | 通过标准 | 验收结果 | 异常说明 |
|----------|--------|----------|----------|----------|----------|
| SHARE-CODE-001 | 项目序列化与反序列化 | 读取`src/store/project.js`，检查是否包含：<br>1. 项目数据序列化为`.promilia`格式的逻辑<br>2. `.promilia`文件解析为项目数据的逻辑<br>3. 数据完整性校验逻辑 | 1. 序列化/反序列化逻辑完整<br>2. 包含所有项目数据：配置、排轴、养成、设置<br>3. 有数据完整性校验，防止篡改 |  |  |
| SHARE-CODE-002 | 多格式导出逻辑 | 检查是否有：<br>1. 高清图片导出逻辑（canvas或html2canvas）<br>2. Markdown报告导出逻辑<br>3. JSON源文件导出逻辑 | 1. 多格式导出逻辑完整<br>2. 图片导出支持分辨率选择<br>3. 所有导出无内容截断 |  |  |
| SHARE-CODE-003 | 预设轴库页面存在性 | 检查`src/views/Preset.vue`文件是否存在 | 文件存在，无缺失 |  |  |
| SHARE-CODE-004 | 预设轴筛选与导入逻辑 | 读取`Preset.vue`：<br>1. 检查是否有预设轴列表渲染逻辑<br>2. 检查是否有按角色/配队/Boss筛选逻辑<br>3. 检查是否有一键导入预设轴逻辑 | 1. 列表与筛选逻辑完整<br>2. 一键导入逻辑复用项目反序列化逻辑<br>3. 无重复代码 |  |  |

### 模块七：编辑器核心能力补全与体验优化（P0核心）
| 验收编号 | 验收项 | 验收操作 | 通过标准 | 验收结果 | 异常说明 |
|----------|--------|----------|----------|----------|----------|
| EDITOR-CODE-001 | 撤销/重做历史记录实现 | 读取`src/store/project.js`，检查是否包含：<br>1. 操作历史数组的state<br>2. 记录操作的action<br>3. undo/redo的action<br>4. 最大步数限制逻辑 | 1. 历史记录逻辑完整<br>2. 支持≥50步记录<br>3. undo/redo后数据完全同步<br>4. 有完整注释 |  |  |
| EDITOR-CODE-002 | 关键帧标记逻辑 | 读取`src/store/project.js`与时间轴组件：<br>1. 检查是否有关键帧标记的state<br>2. 检查是否有添加/编辑/删除标记的逻辑<br>3. 检查是否有标记持久化逻辑 | 1. 标记逻辑完整<br>2. 支持自定义名称、颜色、备注<br>3. 标记保存到localStorage |  |  |
| EDITOR-CODE-003 | 技能块高级操作逻辑 | 读取时间轴组件：<br>1. 检查是否有技能块拆分/合并逻辑<br>2. 检查是否有多选/批量操作逻辑<br>3. 检查是否有批量对齐逻辑 | 1. 高级操作逻辑完整<br>2. 无数据丢失<br>3. 操作有撤销/重做支持 |  |  |
| EDITOR-CODE-004 | 全局设置页面存在性 | 检查`src/views/Setting.vue`文件是否存在 | 文件存在，无缺失 |  |  |
| EDITOR-CODE-005 | 设置状态管理实现 | 读取`src/store/setting.js`，检查是否包含：<br>1. 编辑器、显示、播放、快捷键的state<br>2. 设置修改的action<br>3. 设置持久化到localStorage的逻辑 | 1. 设置状态完整<br>2. 持久化逻辑实现<br>3. 支持数据备份/恢复逻辑 |  |  |
| EDITOR-CODE-006 | 新手引导与教程页面 | 检查：<br>1. `src/views/Guide.vue`是否存在<br>2. 编辑器中是否有新手引导组件 | 1. 教程页面存在，包含基础入门、进阶技巧、FAQ<br>2. 新手引导组件存在，支持分步引导<br>3. 引导可随时重新打开 |  |  |

### 模块八：游戏数据补全（P1）
| 验收编号 | 验收项 | 验收操作 | 通过标准 | 验收结果 | 异常说明 |
|----------|--------|----------|----------|----------|----------|
| DATA-CODE-001 | gamedata.json文件存在性 | 检查`public/gamedata/gamedata.json`文件是否存在 | 文件存在，无缺失 |  |  |
| DATA-CODE-002 | 核心角色数据完整性 | 读取`gamedata.json`：<br>1. 检查`characters`数组长度≥8<br>2. 检查每个角色包含基础属性、普攻、重击、星鸣技、星决技、星携技、被动天赋<br>3. 检查技能参数完整（帧数、CD、倍率、能量、印记等） | 1. 角色数量达标<br>2. 每个角色数据结构完整<br>3. 技能参数无缺失<br>4. 无硬编码，结构可扩展 |  |  |
| DATA-CODE-003 | 敌人与奇波数据完整性 | 读取`gamedata.json`：<br>1. 检查`enemies`数组长度≥4<br>2. 检查每个敌人包含抗性、韧性、机制、虚弱窗口<br>3. 检查`chippo`数组长度≥3 | 1. 敌人与奇波数量达标<br>2. 数据结构完整<br>3. 机制参数无缺失 |  |  |

---

## 三、代码规范与可维护性验收
| 验收编号 | 验收项 | 验收操作 | 通过标准 | 验收结果 | 异常说明 |
|----------|--------|----------|----------|----------|----------|
| STD-CODE-001 | 代码注释完整性 | 随机抽取10个核心函数、5个核心组件：<br>1. 检查是否有JSDoc注释<br>2. 检查复杂逻辑是否有行内注释<br>3. 检查注释是否清晰说明用途 | 1. 核心函数100%有JSDoc注释<br>2. 复杂逻辑有行内注释<br>3. 注释无歧义、与代码一致 |  |  |
| STD-CODE-002 | 无硬编码游戏数值 | 检查核心工具函数与组件：<br>1. 检查是否有游戏数值、机制规则硬编码<br>2. 检查是否均通过`gamedata.json`配置驱动 | 1. 无硬编码游戏数值<br>2. 所有机制通过配置驱动<br>3. 预留扩展字段 |  |  |
| STD-CODE-003 | ESLint与Prettier配置 | 检查：<br>1. `.eslintrc.cjs`是否存在<br>2. `.prettierrc`是否存在<br>3. `package.json`是否有`lint`、`format`脚本 | 1. 配置文件存在<br>2. 脚本存在<br>3. 规则适配Vue3 + Vite |  |  |
| STD-CODE-004 | 常量统一管理 | 检查`src/utils/constants.js`是否存在：<br>1. 检查是否有魔法数字定义为常量<br>2. 检查是否有全局配置常量 | 1. 常量文件存在<br>2. 魔法数字均定义为常量<br>3. 常量命名规范 |  |  |

---

## 四、整体代码验收结论与整改清单
### 1. 整体代码验收结论（仅可选择一项）
□  ✅ 完全通过：所有P0级核心文件/函数100%实现，代码规范符合要求，架构复用性强，无硬编码
□  ⚠️ 有条件通过：P0级核心文件/函数100%存在，存在少量逻辑未实现/注释缺失/规范问题，可快速整改
□  ❌ 不通过：存在P0级核心文件缺失/核心逻辑未实现/架构重构，需重新开发后再次验收

### 2. 核心代码整改清单（不通过/有条件通过必须填写）
| 问题编号 | 对应验收项 | 问题描述 | 整改要求 | 整改优先级 |
|----------|------------|----------|----------|------------|
| CODE-FIX-001 |  |  |  |  |
| CODE-FIX-002 |  |  |  |  |
| CODE-FIX-003 |  |  |  |  |

### 3. 代码优化建议
1. 
2. 
3.